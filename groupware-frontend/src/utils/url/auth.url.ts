const authURL = `auth`;

export const registerURL = `/${authURL}/register`;
export const loginURL = `/${authURL}/login`;
export const authenticateURL = `/${authURL}/authenticate`;
export const logoutURL = `/${authURL}/logout`;
