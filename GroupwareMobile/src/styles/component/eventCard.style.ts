import {StyleSheet} from 'react-native';

export const eventCardStyles = StyleSheet.create({
  image: {
    height: '100%',
    width: '100%',
  },
  tagList: {
    flexDirection: 'row',
    marginHorizontal: 8,
  },
});
