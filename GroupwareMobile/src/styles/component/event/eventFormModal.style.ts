import {StyleSheet} from 'react-native';

export const eventFormModalStyles = StyleSheet.create({
  descriptionInput: {
    backgroundColor: 'white',
    minHeight: 120,
    borderColor: '#e0e0e0',
    borderWidth: 1,
    padding: 8,
    borderRadius: 5,
    fontSize: 16,
  },
});
