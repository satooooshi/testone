import {StyleSheet} from 'react-native';

export const fileMessageStyles = StyleSheet.create({
  messageWrapper: {
    backgroundColor: '##F2F5FB',
    // borderColor: '#edf2f7',
    // borderWidth: 1,
    borderRadius: 10,
    height: 160,
    aspectRatio: 1,
    alignItems: 'center',
    justifyContent: 'center',
    padding: 8,
  },
});
