import {StyleSheet} from 'react-native';

export const eventDetailStyles = StyleSheet.create({
  image: {
    // width must be set 90% of window by dimention
    marginBottom: 8,
    width: '100%',
    // marginLeft: 'auto',
    // marginRight: 'auto',
  },
});
