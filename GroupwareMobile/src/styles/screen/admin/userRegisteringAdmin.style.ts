import {StyleSheet} from 'react-native';

export const userRegisteringAdminStyles = StyleSheet.create({
  scrollView: {
    alignSelf: 'center',
  },
  textArea: {
    fontSize: 16,
    height: 100,
    backgroundColor: 'white',
    borderRadius: 10,
    borderColor: '#e0e0e0',
    borderWidth: 1,
    padding: 8,
  },
});
